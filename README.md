# Real Estate Viewings API

The API includes bounded availability lookup, lifecycle actions, Sanctum
authentication, throttling, and explicit response resources.

Team-scoped viewing booking API. Composer is unprefixed; GitHub source uses `module-real-estate-viewings-api`.
